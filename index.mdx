---
title: "Lumera Protocol"
sidebarTitle: "Introduction"
description: "Infrastructure for permanent, provable data and decentralized intelligent services."
---

Lumera is a decentralized protocol that enables enterprises and developers to build applications on permanent, provable data and decentralized intelligent services. By combining permanent data, verifiable execution, and long-lived digital state within a common protocol framework, Lumera provides infrastructure that remains transparent, auditable, and independent of centralized providers.

Unlike general-purpose blockchains, Lumera is built around a simple architectural principle: consensus should secure the network, not perform every service. Validators maintain consensus and governance, while economically bonded SuperNodes provide storage, verification, retrieval, and decentralized execution. This separation allows sophisticated services to scale independently without compromising security or decentralization.

The blockchain serves as the network's coordination layer, recording requests, commitments, payments, proofs, and governance, while SuperNodes perform the underlying work and return cryptographic evidence of completion. Rather than forcing every computation into consensus, Lumera combines the scalability of distributed systems with the trust guarantees of blockchain infrastructure.

Lumera is a protocol designed to support an expanding ecosystem of decentralized intelligent services built on a shared architectural foundation. As the network evolves, each new capability extends the protocol through the same architectural principles, execution model, and economic framework—creating a unified platform where permanent storage, verification, identity, memory, execution, and settlement reinforce one another rather than exist as independent systems.

## Key Features

<CardGroup cols={2}>
  <Card title="Permanent, Provable Storage" icon="database">
    Store data once and retain it indefinitely. SuperNodes continuously prove they still hold the data they are responsible for, with cryptographic proofs recorded on chain.
  </Card>

  <Card title="Validator-SuperNode Architecture" icon="server">
    Validators secure consensus while dedicated SuperNodes provide storage, verification, retrieval, and decentralized services, allowing execution to scale independently of consensus.
  </Card>

  <Card title="Composable Services" icon="diagram-project">
    Every decentralized intelligent service is delivered as a modular protocol service with a common lifecycle for registration, execution, settlement, and verification. New services extend the protocol without introducing separate systems.
  </Card>

  <Card title="Sustainable Protocol Economics" icon="chart-line">
    Protocol fees compensate network operators, sustain long-lived services, and align economic incentives between developers, users, validators, and SuperNodes.
  </Card>

  <Card title="Interoperable by Design" icon="arrows-left-right">
    Build with familiar EVM tooling, integrate across the Cosmos ecosystem through IBC, or consume Lumera's services without migrating existing applications.
  </Card>

  <Card title="On-Chain Governance" icon="landmark">
    LUME holders govern protocol parameters, service economics, treasury allocation, and future network upgrades through decentralized governance.
  </Card>
</CardGroup>

## Design Principle

Lumera is built around a simple architectural principle:

> **Reason off-chain. Verify on-chain.**

**Consensus should establish truth—not perform every computation.**

The blockchain acts as the protocol's control plane, recording commitments, permissions, payments, governance, and cryptographic proofs, while SuperNodes perform storage, verification, retrieval, and decentralized services.

By separating consensus from execution, Lumera allows sophisticated protocol services to scale independently while ensuring every operation can produce verifiable evidence rather than requiring users to trust the party performing the work.

<img
  className="block mx-auto dark:hidden"
  alt="Your app sends a request and fee to the control plane. Data moves directly to the execution plane without passing through consensus. SuperNodes return evidence of completion to the chain."
  src="/images/diagrams/protocol-planes-light.svg"
/>
<img
  className="mx-auto hidden dark:block"
  alt="Your app sends a request and fee to the control plane. Data moves directly to the execution plane without passing through consensus. SuperNodes return evidence of completion to the chain."
  src="/images/diagrams/protocol-planes-dark.svg"
/>

## The Action and Agent framework

Every decentralized intelligent service on Lumera is implemented through a common execution framework built around **Actions** and **Agents**. Together, they provide a consistent model for how protocol services are requested, executed, verified, and settled across the network.

**Actions** are the atomic building blocks of decentralized intelligent services. Each Action represents a single verifiable unit of work executed by SuperNodes, following a common lifecycle for registration, execution, settlement, and proof generation. Because every Action follows the same framework, new protocol services extend the network without introducing new execution models.

### Example: Cascade

Cascade is an Action that permanently stores data using erasure coding, decentralized SuperNode storage, and continuous cryptographic retention proofs.

**Agents** build on top of Actions. Rather than performing work directly, Agents coordinate and compose one or more Actions into higher-level decentralized intelligent services, allowing applications to orchestrate storage, verification, computation, and future protocol services through a single execution framework.

This layered architecture allows Lumera to evolve through composition rather than reinvention. Every new decentralized intelligent service shares the same execution model, verification framework, and settlement lifecycle, creating a unified protocol where services remain interoperable by design.

## Building on Lumera

Applications built on Lumera consume protocol services through a common execution framework. Today, developers can permanently store data using Cascade, with every file erasure-coded, distributed across economically bonded SuperNodes, and continuously verified through cryptographic retention proofs. Rather than relying on a centralized storage provider, applications receive permanent, verifiable data backed by the protocol itself.

This foundation enables developers to build applications that require long-lived data, verifiable infrastructure, and decentralized execution without managing storage networks or trusted intermediaries. As additional protocol services are introduced, they become available through the same framework and developer experience.

<CardGroup cols={3}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Store your first file from TypeScript in just a few minutes.
  </Card>

  <Card title="How Cascade works" icon="database" href="/cascade/how-cascade-works">
    Learn how permanent storage, fees, and retention proofs work.
  </Card>

  <Card title="SDKs" icon="code" href="/sdk/javascript">
    Build using TypeScript, Go, or Rust.
  </Card>
</CardGroup>

## Protocol Architecture

Lumera separates consensus from service execution through a Validator–SuperNode architecture.

Validators secure the network through delegated Proof of Stake, proposing and validating blocks while governing protocol parameters. SuperNodes provide the protocol services applications consume, including storage, verification, retrieval, and decentralized execution. A SuperNode remains active only while bonded to an eligible validator, ensuring service capacity is backed by real economic commitment.

The blockchain serves as the protocol's control plane, recording requests, commitments, payments, governance, and cryptographic proofs. SuperNodes serve as the execution and memory layers, performing work directly for clients without routing data through consensus. LUME powers staking, transaction fees, governance, and protocol services, aligning the network's economic incentives around a single native asset.

<CardGroup cols={2}>
  <Card title="Architecture" icon="sitemap" href="/architecture">
    Learn how validators, SuperNodes, modules, and clients interact.
  </Card>

  <Card title="LUME" icon="coins" href="/token">
    Explore token utility, supply mechanics, and staking.
  </Card>

  <Card title="Protocol economics" icon="receipt" href="/protocol-revenue">
    Understand protocol revenue, fee distribution, and sustainability.
  </Card>

  <Card title="Governance" icon="landmark" href="/governance">
    Learn how protocol upgrades and parameter changes are governed.
  </Card>
</CardGroup>

## Lumera Vision

Lumera is designed as a long-lived protocol rather than a collection of independent products. The same architectural principles that enable permanent, provable data naturally extend to durable memory, decentralized identity, verifiable execution, settlement, trust, and future decentralized intelligent services.

Rather than introducing separate systems for each new capability, Lumera extends a common execution framework, economic model, and security architecture. Every new protocol service builds upon the same foundation, allowing the network to evolve through composition rather than reinvention.

The result is a unified protocol where storage, memory, identity, verification, execution, settlement, and intelligent services reinforce one another through a consistent architectural model—providing developers with a stable platform that becomes more capable over time without requiring them to rebuild on a different foundation.